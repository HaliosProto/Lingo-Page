# Defensive security baseline

Audit date: 2026-07-18. Scope: ordinary single-pass source/configuration review plus existing repository scans. No external service, credential, production environment, exploitation, attack simulation, or specialized security workspace was used.

## Commands and inspection completed

- `pnpm security:scan`: passed using the installed workspace runtime. Two extension roots and one configured secret value were compared without displaying the value.
- Production dependency audit: attempted offline and could not complete because the package manager still required registry access, which was intentionally unavailable. The previous dated reports are historical evidence only.
- `.gitignore`/tracking: `apps/api/.dev.vars` is ignored and untracked; build, release, local log, environment, and secret patterns are ignored.
- Source review: permissions/manifests, message/request validation, provider keys/routes, CORS/origins, limits, storage/cache/diagnostics/logging, DOM insertion, navigation/tab/session isolation, provider URL policy, and release scripts.
- Generated manifest/bundle/source-map inspection is rerun as part of the Milestone 0 verification report after the production build.

## Existing protections

- Required MV3 permissions are limited to `activeTab`, `contextMenus`, `scripting`, and `storage`; required production host access resolves to the application API, with the fixture origin only in E2E mode. The later translated-copy acceptance fix declares optional HTTP/HTTPS patterns and grants only the user-selected current origin.
- Provider credentials and arbitrary endpoints are backend-owned. Provider/model IDs are registry/allowlist validated and selected-provider failure does not silently change recipient.
- Runtime Zod schemas validate extension messages, API bodies, and provider results. Content-script translation/progress senders require the extension ID, a tab, and top frame.
- Provider output reaches the page through `Text.nodeValue` or `textContent`; no provider HTML is interpreted.
- Request IDs, navigation/session checks, cancellation epochs, connected-node checks, and exact originals reduce stale/cross-navigation mutation.
- Persistent translation cache is opt-in, capped, clearable, and disabled in privacy mode. Diagnostics/logs are designed as bounded metadata.
- API routes set no-store, no-referrer, framing, MIME, permissions, request-ID, and allowlisted-origin headers and enforce schema/segment/character/rate/quota/concurrency controls.

## Findings

No confirmed critical or high-severity vulnerability was established by this bounded review. Findings below are prioritized for this architecture and are not exploit claims.

| Code path                                                     | Defensive impact                                                                                                                                                                                                             | Confidence                                         | Safe remediation                                                                                                                              | Recommended test                                                                                                                               |
| ------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- |
| `apps/extension/entrypoints/page-shell.ts:60-77,128-142`      | The exclusion selector only matches `contenteditable="true"`; empty, `plaintext-only`, or inherited editable regions may be treated as translatable and disclose draft text after explicit page translation.                 | Plausible                                          | Reject when the parent or an ancestor is editable using platform semantics such as `isContentEditable`, retaining explicit form exclusions.   | Synthetic fixtures for empty/true/plaintext-only/inherited editability; assert no segment leaves the page.                                     |
| `apps/extension/entrypoints/page-shell.ts:123-142`            | Visibility checks only the immediate parent’s computed style; a hidden ancestor can make text non-visible while its child appears locally visible.                                                                           | Plausible                                          | Use an ancestor-aware/rendered visibility policy with bounded cost and document opacity handling.                                             | Hidden/display/visibility/opacity ancestors, closed details, offscreen and clipped cases; assert intended eligibility.                         |
| `apps/extension/entrypoints/page-shell.ts:128-142`            | `surroundingText` uses the entire parent `textContent`, so text in an otherwise excluded descendant may be included as context even when its own node is skipped.                                                            | Confirmed code path; exposure depends on DOM shape | Construct context only from eligible text records or omit the field until its benefit is measured.                                            | Parent containing normal text plus password/editable/code/notranslate descendants; inspect the validated backend request without logging text. |
| `apps/api/src/index.ts:157-170,296-302`                       | The application byte limit is checked after `req.text()` buffers the body; absent or inaccurate `Content-Length` can consume more memory before rejection. Platform ingress limits may reduce impact but were not validated. | Plausible                                          | Enforce a platform/streaming body cap before full buffering and keep the post-decode check.                                                   | Mocked chunked/absent/incorrect length requests at boundary sizes; verify early 413 and bounded allocation.                                    |
| `packages/translation-providers/src/runtime.ts:145-171`       | The timeout is disposed after response headers, before JSON body consumption, and no response-byte cap exists. A slow or oversized provider body may outlive the intended timeout or consume excess memory.                  | Plausible                                          | Keep the abort scope through bounded body reading/parsing; cap bytes and normalize timeout/oversize failures.                                 | Mock Response with delayed body and oversized chunks; assert abort/typed failure without active external calls.                                |
| `packages/translation-providers/src/registry.ts:57-103`       | Custom endpoints reject literal private IPv4 and `.local`, but do not demonstrate DNS rebinding/redirect/private IPv6 revalidation. This matters only when a backend operator enables the advanced custom provider.          | Plausible, configuration-dependent                 | Resolve/revalidate destinations at connection time, cover IPv6/reserved ranges, control redirects, and prefer an explicit operator allowlist. | Pure URL-policy tests plus mocked resolver/redirect cases; no network probing.                                                                 |
| `apps/api/src/index.ts:55-125,78`                             | Local rate/quota identity can rely on `Origin`; counters are in-memory, non-atomic, and reset on Worker restart. They are not production abuse or billing enforcement.                                                       | Informational / confirmed limitation               | Replace with authenticated principal plus trusted network signal and durable atomic accounting before production.                             | Concurrency, spoofed-header-at-unit-boundary, restart, quota and revocation integration tests.                                                 |
| `packages/translation-core/src/index.ts:45-67`                | Persistent cache keys use a 32-bit FNV-1a digest without storing/verifying canonical key material; an unintended collision can return the wrong translation.                                                                 | Plausible, low likelihood                          | Store normalized key fields alongside the value and compare them, or use a stronger available digest with collision verification.             | Deterministic collision/canonical-mismatch unit test using an injected hash seam.                                                              |
| `scripts/build-release-candidate.ps1:32-45`                   | Packaging rejects only the exact `<all_urls>` token and records `HEAD` even if tracked files are dirty. A broader wildcard permission or dirty build could be mislabeled as an approved commit artifact.                     | Confirmed release-gate gap                         | Allowlist the exact expected host permissions, fail on tracked/staged diff, or record dirty state and diff hash.                              | Script tests with synthetic manifests and dirty tracked file; assert packaging stops.                                                          |
| `scripts/start-local-test.ps1`, `scripts/stop-local-test.ps1` | PID-file shutdown uses a numeric PID and can terminate the wrong reused process; readiness proves a matching health marker but not necessarily ownership by the launched process.                                            | Plausible local-safety issue                       | Store and verify process start time/command/port ownership and use a per-run nonce before force stopping.                                     | Mocked stale/reused PID and occupied-port cases; assert unrelated process is never targeted.                                                   |

## Gaps in tests and documentation

- Editable/hidden-ancestor and excluded-descendant-context fixtures.
- Provider response-size and body-timeout tests.
- Comprehensive custom endpoint IPv6/DNS/redirect policy tests.
- Release-script allowlist/dirty-provenance tests.
- Service-worker restart and SPA navigation-race tests.
- Production identity, durable quotas, retention, deletion, and trusted-client CORS/auth design.
- Current production dependency advisory result while offline.

## Milestone 1 addendum

The 2026-07-18 durable-session change received a bounded defensive review using the repository scan and normal source/artifact inspection. No specialized security workspace, external scanning, credential testing, active validation, or exploit work was used.

- New message and bundle payloads are runtime validated. Bundles are versioned, capped at 2,500 segments and 2 MB, and restricted to HTTP(S) navigation identities.
- Privileged popup/comparison commands now require the extension's own page origin in addition to the extension ID. Content-script progress/translation messages retain top-frame tab checks. This closes a plausible sender-authorization gap discovered during review; legitimate flows passed managed Chromium E2E. Add a direct fake-sender router test when the background router gains an injectable test seam.
- Comparison handoff uses an unguessable fragment token, `chrome.storage.session`, an owning-tab binding, and single-use retrieval/cleanup. Invalid, stale, mismatched, or oversized payloads fail closed.
- Copy import validates navigation and applies only confident plain-text matches. Uncertain content remains original. Ending one page-owned session does not clear another tab.
- At the time of this baseline, production required permissions remained `activeTab`, `contextMenus`, `scripting`, and `storage`, with only the loopback application API host. The later translated-copy fix adds optional HTTP/HTTPS declarations without required `<all_urls>` or automatic site access; see ADR 0009 and the Milestone 1 verification addendum.
- Generated extension and Worker bundles were scanned without displaying configured values. No credential-pattern match was found. Production extension source maps are absent; the Worker dry-run source map embeds expected source content and is confined to ignored build output.
- The production dependency advisory refresh was unavailable because registry access was prohibited. An offline attempt did not complete and was terminated; it is not reported as a pass.

The earlier editable/hidden-ancestor/context-construction findings are remediated in the page-shell eligibility and bounded structural-context paths and covered by regression fixtures. The backend body cap, provider-body timeout/size, custom-endpoint network policy, release provenance, local PID ownership, durable production identity/quota, and cache-collision items remain separate follow-up work.

## Recommended dedicated follow-up

Handle the confirmed context-construction issue and the body/timeout/release gates as narrow Milestone 1 or release-readiness tasks after validation. Re-run the ordinary dependency audit when approved registry access is available. A future dedicated security milestone should validate identity, durable accounting, custom-provider network policy, and release provenance after those architectures exist; it should remain scoped and evidence-based.
